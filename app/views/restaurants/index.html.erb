<h1>Restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <h2><%= restaurant.name %></h2>
  <p><%= restaurant.address %></p>
  <p><%= link_to "see", restaurant_path(restaurant) %></p>
<% end %>

<p><%= link_to "Add a new restaurant", new_restaurant_path %></p>
